package main

import (
	"fmt"
	// "github.com/singoesdeep/templater" // Uncomment after initial push
)

func main() {
	fmt.Println("Templater example: Please uncomment the import and usage after pushing to GitHub.")
	// Example usage (uncomment after initial push):
	// data, err := templater.LoadData("data.json")
	// if err != nil {
	//     log.Fatal(err)
	// }
	// result, err := templater.Render("template.tmpl", data)
	// if err != nil {
	//     log.Fatal(err)
	// }
	// fmt.Println(result)
}
